# CFTun-UI 开发方案

## 1. 项目目标

开发一个运行在 Docker 中的 Cloudflare Tunnel 可视化管理后台。

用户在后台配置 Cloudflare API Token、Account、Zone、Tunnel 和 Cloudflare Access 后，可以通过页面创建和管理以下代理映射：

1. 宿主机服务，例如 `127.0.0.1:2639`。
2. 宿主机所在局域网内的设备，例如 `192.168.1.20:5000`。

项目支持：

- Linux
- macOS
- Windows

所有平台统一使用 Docker Host Network，不区分多套网络配置，不提供 Bridge 模式回退。

## 2. 最终技术方案

```text
运行环境：Docker
容器网络：network_mode: host
容器结构：Bun + cloudflared 单容器
后端运行时：Bun + TypeScript
前端：React + Bun HTML imports
数据库：bun:sqlite
Cloudflare 配置：Cloudflare REST API v4
Tunnel 类型：远程托管 Tunnel
Connector：Bun.spawn(cloudflared)
Token 存储：SQLite 明文保存
支持目标：宿主机、宿主机局域网设备
首版协议：HTTP、HTTPS
```

不支持：

- Docker Bridge 网络模式
- `host.docker.internal`
- 多套 Compose 文件
- 本地托管 Tunnel
- `cert.pem` 和本地 `config.yml`
- Docker 容器发现或容器间代理
- 任意 Docker 命令和 Docker Socket
- 首版中的 SSH、RDP、SMB、裸 TCP 和数据库客户端接入

## 3. 总体架构

```text
浏览器
   |
   | HTTP/HTTPS
   v
┌─────────────────────────────────────────────┐
│ CFTun-UI Docker 容器                        │
│ network_mode: host                          │
│                                             │
│  Bun 后台 + React UI                        │
│    ├── Cloudflare REST API Client           │
│    ├── SQLite                               │
│    ├── 映射管理服务                         │
│    ├── Access 管理服务                      │
│    └── Connector 进程管理器                 │
│                    |                        │
│                    v                        │
│  cloudflared 子进程                         │
└────────────────────┬────────────────────────┘
                     |
                     ├── http://127.0.0.1:2639
                     │      宿主机服务
                     |
                     ├── http://192.168.1.20:5000
                     │      局域网设备
                     |
                     └── https://192.168.1.30:8443
                            局域网设备
```

Bun 和 `cloudflared` 位于同一个容器、共享同一个 Host Network，因此 Bun 的目标连通性测试与 `cloudflared` 的网络视角一致。

## 4. Host Network 统一规则

Docker Compose 固定使用：

```yaml
network_mode: host
```

等价 Docker 命令：

```bash
docker run --network host ...
```

Host Network 模式下不配置 `ports`，因为容器直接使用宿主机网络。

### 4.1 宿主机服务

宿主机服务直接使用 loopback 地址：

```text
http://127.0.0.1:<PORT>
https://127.0.0.1:<PORT>
```

示例：

```json
{
  "hostname": "app.example.com",
  "service": "http://127.0.0.1:2639"
}
```

### 4.2 局域网设备

局域网设备直接使用其 LAN IP 或可解析的局域网 hostname：

```text
http://192.168.1.20:5000
https://nas.lan:8443
```

示例：

```json
{
  "hostname": "nas.example.com",
  "service": "http://192.168.1.20:5000"
}
```

局域网目标需要满足：

- 宿主机能够访问目标设备。
- 目标设备服务监听局域网 IP，而不是只监听目标设备自身的 `127.0.0.1`。
- 目标设备防火墙允许宿主机访问相应端口。
- 目标设备最好配置静态 IP 或 DHCP 地址保留。

## 5. 跨平台前置要求

### 5.1 Linux

Linux 原生支持 Docker Host Network，无需额外设置。

### 5.2 macOS 和 Windows

macOS 和 Windows 必须满足：

- 安装支持 Host Networking 的 Docker Desktop，最低使用 Docker Desktop 4.34 或更高版本。
- 使用 Linux Containers。
- 在 Docker Desktop 中显式启用 Host Networking。

设置路径：

```text
Docker Desktop
-> Settings
-> Resources
-> Network
-> Enable host networking
```

启用后重启 Docker Desktop。

如果 Host Networking 未启用或不可用，程序应明确显示环境错误并停止 Connector，不使用其他网络模式自动回退。

### 5.3 部署前验证

验证宿主机端口：

```bash
docker run --rm --network host curlimages/curl \
  --connect-timeout 5 http://127.0.0.1:2639
```

验证局域网设备：

```bash
docker run --rm --network host curlimages/curl \
  --connect-timeout 5 http://192.168.1.20:5000
```

测试成功后，`cloudflared` 可以使用相同的地址作为 ingress service。

## 6. Docker 镜像

镜像内同时包含 Bun 应用和 `cloudflared`。

```dockerfile
FROM cloudflare/cloudflared:<固定版本> AS cloudflared

FROM oven/bun:<固定版本>

COPY --from=cloudflared \
  /usr/local/bin/cloudflared \
  /usr/local/bin/cloudflared

WORKDIR /app

COPY package.json bun.lock ./
RUN bun install --frozen-lockfile --production

COPY . .

ENV SERVER_HOST=127.0.0.1
ENV SERVER_PORT=6611
ENV DATA_DIR=/data

CMD ["bun", "run", "index.ts"]
```

正式实现应固定 Bun 和 `cloudflared` 镜像版本，不长期使用 `latest`，以保证构建可复现。

## 7. Docker Compose

项目只提供一份 Compose 文件：

```yaml
services:
  cftun-ui:
    build: .
    image: cftun-ui:latest
    container_name: cftun-ui
    network_mode: host
    restart: unless-stopped
    environment:
      SERVER_HOST: 127.0.0.1
      SERVER_PORT: 6611
      DATA_DIR: /data
    volumes:
      - cftun-data:/data

volumes:
  cftun-data:
```

启动：

```bash
docker compose up -d
```

查看日志：

```bash
docker compose logs -f cftun-ui
```

本地后台地址：

```text
http://127.0.0.1:6611
```

## 8. Cloudflare 管理模型

仅支持远程托管 Tunnel：

```json
{
  "config_src": "cloudflare"
}
```

除运行 Connector 外，所有 Cloudflare 配置都通过 REST API 完成，不解析 `cloudflared` CLI 输出。

主要 API：

```text
GET    /user/tokens/verify
GET    /accounts
GET    /zones
GET    /zones/{zone_id}/dns_records
POST   /zones/{zone_id}/dns_records
PUT    /zones/{zone_id}/dns_records/{dns_record_id}
DELETE /zones/{zone_id}/dns_records/{dns_record_id}

GET    /accounts/{account_id}/tunnels
POST   /accounts/{account_id}/cfd_tunnel
GET    /accounts/{account_id}/cfd_tunnel/{tunnel_id}/token
GET    /accounts/{account_id}/cfd_tunnel/{tunnel_id}/configurations
PUT    /accounts/{account_id}/cfd_tunnel/{tunnel_id}/configurations

GET    /accounts/{account_id}/access/apps
POST   /accounts/{account_id}/access/apps
PUT    /accounts/{account_id}/access/apps/{app_id}
DELETE /accounts/{account_id}/access/apps/{app_id}
POST   /accounts/{account_id}/access/apps/{app_id}/policies
```

具体请求和响应参考项目根目录的 [`TUN-API.md`](./TUN-API.md)。

## 9. 初始化流程

首次访问：

```text
http://127.0.0.1:6611
```

初始化步骤：

1. 输入 Cloudflare API Token。
2. 调用 `GET /user/tokens/verify` 验证 Token。
3. Token 明文保存到 SQLite。
4. 获取并选择 Cloudflare Account。
5. 获取并选择 Zone。
6. 选择已有远程托管 Tunnel，或创建新 Tunnel。
7. 获取 Tunnel token，并明文保存到 SQLite。
8. Bun 启动 `cloudflared` Connector。
9. 可选配置后台自身的公网 hostname。
10. 为后台 hostname 创建 DNS、Tunnel ingress、Access application 和 policy。

后台自身与 `cloudflared` 在同一个容器中，因此后台 ingress 固定为：

```json
{
  "hostname": "tunnel-admin.example.com",
  "service": "http://127.0.0.1:6611"
}
```

## 10. cloudflared 进程管理

Bun 是容器主进程，`cloudflared` 是 Bun 启动的子进程。

```ts
const connector = Bun.spawn({
  cmd: [
    "/usr/local/bin/cloudflared",
    "tunnel",
    "--no-autoupdate",
    "run",
    "--token",
    tunnelToken,
  ],
  stdout: "pipe",
  stderr: "pipe",
});
```

进程管理器负责：

- 启动 Connector。
- 停止 Connector。
- 重启 Connector。
- 读取并推送日志。
- 记录进程退出码。
- 异常退出后自动重启。
- 容器结束时向 Connector 发送 `SIGTERM`。

推荐重试间隔：

```text
1 秒 -> 2 秒 -> 5 秒 -> 10 秒 -> 30 秒
```

之后保持每 30 秒重试一次。用户主动停止 Connector 时不自动重启。

远程 ingress 更新通常不需要重启 `cloudflared`。

## 11. 支持的映射类型

### 11.1 宿主机映射

用户输入：

```text
Hostname: app.example.com
目标类型: 宿主机
协议: HTTP
端口: 2639
```

保存并提交：

```json
{
  "hostname": "app.example.com",
  "targetType": "host",
  "protocol": "http",
  "targetHost": "127.0.0.1",
  "targetPort": 2639,
  "service": "http://127.0.0.1:2639"
}
```

### 11.2 局域网设备映射

用户输入：

```text
Hostname: nas.example.com
目标类型: 局域网设备
协议: HTTP
地址: 192.168.1.20
端口: 5000
```

保存并提交：

```json
{
  "hostname": "nas.example.com",
  "targetType": "lan",
  "protocol": "http",
  "targetHost": "192.168.1.20",
  "targetPort": 5000,
  "service": "http://192.168.1.20:5000"
}
```

### 11.3 HTTPS 和自签名证书

```json
{
  "hostname": "router.example.com",
  "service": "https://192.168.1.1:443",
  "originRequest": {
    "noTLSVerify": true
  }
}
```

后台提供以下高级选项：

- 禁用 origin TLS 证书验证。
- `originServerName`。
- `httpHostHeader`。
- Path 匹配。

## 12. 映射表单

### 公网配置

```text
Zone
子域名
完整 hostname 预览
可选 path
```

### 目标配置

```text
目标类型
  - 宿主机
  - 局域网设备

协议
  - HTTP
  - HTTPS

目标地址
目标端口
```

选择“宿主机”时，目标地址固定为 `127.0.0.1`，用户只填写端口。

选择“局域网设备”时，用户填写 LAN IP 或 hostname 和端口。

### Access

```text
不启用 Access
允许指定邮箱
允许指定邮箱域
允许 Access Group
允许 Service Token
```

## 13. 目标地址构建

不进行平台转换：

```ts
type MappingTarget = {
  type: "host" | "lan";
  protocol: "http" | "https";
  host?: string;
  port: number;
};

function buildService(target: MappingTarget): string {
  const host = target.type === "host" ? "127.0.0.1" : target.host;

  if (!host) {
    throw new Error("局域网目标地址不能为空");
  }

  return `${target.protocol}://${host}:${target.port}`;
}
```

输入校验：

- 端口必须是 `1-65535` 的整数。
- 协议只允许 `http` 和 `https`。
- 宿主机地址固定为 `127.0.0.1`。
- 局域网地址允许 IPv4、IPv6 或合法 hostname。
- hostname 必须属于当前选择的 Cloudflare Zone。
- path 必须是合法的 ingress path 表达式。

## 14. 创建映射流程

```text
1. 校验 hostname 属于当前 Zone
2. 构建 origin service URL
3. 从 Bun 容器测试目标连通性
4. 精确查询同名 DNS 记录
5. GET 最新 Tunnel configuration
6. 在 catch-all 规则前插入或更新 ingress
7. PUT 完整 Tunnel configuration
8. 创建或更新 CNAME
9. 可选创建 Access application
10. 可选创建 Access policy
11. 重新查询 Tunnel、DNS 和 Access 状态
12. 保存 Cloudflare 返回的资源 ID
```

DNS CNAME：

```json
{
  "type": "CNAME",
  "name": "app.example.com",
  "content": "<TUNNEL_ID>.cfargotunnel.com",
  "proxied": true,
  "ttl": 1,
  "comment": "Managed by CFTun-UI"
}
```

Tunnel Configuration API 使用全量 `PUT`。实现必须：

```text
GET 最新完整配置
-> 保留已有 ingress 和未知字段
-> 插入、更新或删除目标规则
-> 保留最后的 catch-all
-> PUT 完整配置
-> GET 重新验证
```

不得只提交当前新增的一条 ingress，否则会覆盖已有映射。

## 15. 连通性检查

由于 Bun 和 `cloudflared` 位于同一个 Host Network 容器内，Bun 可以直接测试 Connector 将要访问的地址。

测试分两层：

1. TCP 连接测试。
2. HTTP/HTTPS 请求测试。

状态分类：

```text
TCP 不可达
TCP 可达但 HTTP 请求失败
HTTP 服务已响应
HTTPS 证书错误
连接超时
DNS 解析失败
```

HTTP 返回 `401`、`403` 或 `404` 仍可判定 origin 已响应，不能简单把非 `2xx` 都标记为网络不可达。

## 16. Cloudflare Access

每条映射可以选择是否启用 Access。

启用时创建：

1. Self-hosted Access application。
2. 明确的 Allow policy。
3. 可选的 Tunnel origin JWT 校验。

Access application 示例：

```json
{
  "name": "Protect app.example.com",
  "type": "self_hosted",
  "domain": "app.example.com",
  "session_duration": "24h"
}
```

邮箱 Allow policy 示例：

```json
{
  "name": "Allow administrators",
  "decision": "allow",
  "precedence": 1,
  "include": [
    {
      "email": {
        "email": "admin@example.com"
      }
    }
  ],
  "exclude": [],
  "require": []
}
```

## 17. SQLite 数据结构

### 17.1 `settings`

```sql
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

保存：

```text
cloudflare_api_token
account_id
zone_id
zone_name
tunnel_id
tunnel_name
tunnel_token
access_team_name
setup_completed
connector_enabled
```

Cloudflare API Token 和 Tunnel token 按需求明文保存。

### 17.2 `mappings`

```sql
CREATE TABLE mappings (
  id TEXT PRIMARY KEY,
  hostname TEXT NOT NULL,
  path TEXT,
  target_type TEXT NOT NULL,
  protocol TEXT NOT NULL,
  target_host TEXT NOT NULL,
  target_port INTEGER NOT NULL,
  service TEXT NOT NULL,
  tunnel_id TEXT NOT NULL,
  zone_id TEXT NOT NULL,
  dns_record_id TEXT,
  access_app_id TEXT,
  access_policy_id TEXT,
  access_aud TEXT,
  access_enabled INTEGER NOT NULL DEFAULT 0,
  enabled INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

### 17.3 `operations`

```sql
CREATE TABLE operations (
  id TEXT PRIMARY KEY,
  action TEXT NOT NULL,
  mapping_id TEXT,
  status TEXT NOT NULL,
  message TEXT,
  created_at TEXT NOT NULL
);
```

### 17.4 `connector_logs`

首版优先使用固定大小的内存 ring buffer。若需要持久日志，再增加：

```sql
CREATE TABLE connector_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  level TEXT,
  message TEXT NOT NULL,
  created_at TEXT NOT NULL
);
```

持久日志应设置数量或时间上限，避免数据库无限增长。

## 18. Bun 后台 API

```text
GET    /api/status

GET    /api/setup
POST   /api/setup/token
POST   /api/setup/account
POST   /api/setup/zone
POST   /api/setup/tunnel
POST   /api/setup/complete

GET    /api/cloudflare/accounts
GET    /api/cloudflare/zones
GET    /api/cloudflare/tunnels
POST   /api/cloudflare/tunnels

GET    /api/connector
POST   /api/connector/start
POST   /api/connector/stop
POST   /api/connector/restart
GET    /api/connector/logs
GET    /api/connector/events

GET    /api/mappings
POST   /api/mappings/test
POST   /api/mappings
PUT    /api/mappings/:id
DELETE /api/mappings/:id

GET    /api/access/apps
GET    /api/operations
```

Connector 日志和状态使用 SSE：

```text
GET /api/connector/events
```

## 19. 后端模块

建议目录：

```text
src/
  server.ts
  db.ts
  config.ts

  cloudflare/
    client.ts
    accounts.ts
    zones.ts
    dns.ts
    tunnels.ts
    access.ts

  connector/
    manager.ts
    logs.ts

  mappings/
    service.ts
    target.ts
    health.ts

  routes/
    setup.ts
    cloudflare.ts
    connector.ts
    mappings.ts
    access.ts

frontend/
  index.html
  app.tsx
  styles.css
```

模块职责：

| 模块 | 职责 |
| --- | --- |
| `CloudflareClient` | API 鉴权、请求、分页、错误解析 |
| `TunnelService` | Tunnel 创建、查询、configuration 合并 |
| `DnsService` | CNAME 查询、创建、更新、删除 |
| `AccessService` | Access app 和 policy 管理 |
| `ConnectorManager` | `cloudflared` 子进程及重试 |
| `MappingService` | ingress、DNS、Access 的组合操作 |
| `TargetService` | 目标地址构建和输入校验 |
| `HealthService` | TCP、HTTP、Tunnel 和公网状态检查 |

## 20. 页面结构

### 初始化

```text
API Token
Account
Zone
Tunnel
后台自身域名和 Access
```

### 概览

```text
Tunnel 状态
Connector 状态
映射总数
异常映射
最近操作
```

### 映射

```text
映射列表
添加映射
编辑映射
连通性测试
启用/停用
删除
```

### Access

```text
Access 应用
Allow policies
邮箱、邮箱域、Group、Service Token
```

### 日志

```text
cloudflared 实时日志
后台操作日志
```

### 设置

```text
Cloudflare API Token
Account
Zone
Tunnel
Connector 启动状态
```

## 21. 映射状态聚合

后台不能只显示一条简单的启用状态，应聚合：

```text
目标服务可达
Tunnel ingress 存在
DNS CNAME 正确
Connector Healthy
Access 已配置或明确未启用
公网检查已响应
```

可能状态：

```text
正常
目标不可达
缺少 ingress
缺少 DNS
DNS 指向其他 Tunnel
Connector 离线
Access 无 policy
公网请求失败
Cloudflare 配置与本地数据库不一致
```

## 22. 删除映射

删除页面提供三个独立选项：

```text
删除 Tunnel ingress
删除 DNS CNAME
删除 Access application
```

默认全部选中，但只删除 SQLite 中保存了明确 Cloudflare ID 的资源。

执行顺序：

```text
1. GET 最新 Tunnel configuration
2. 删除目标 ingress
3. PUT 完整 Tunnel configuration
4. 删除对应 DNS record
5. 删除对应 Access application/policy
6. 删除本地 mapping
7. 重新同步状态
```

如果中间步骤失败，保留本地记录并标记部分失败，以便重试，不静默丢失资源 ID。

## 23. 失败处理

Tunnel ingress、DNS 和 Access 是三个独立资源，不存在跨资源事务。

处理原则：

- 每一步记录操作结果和新建资源 ID。
- 只回滚当前操作明确创建的资源。
- 不根据 hostname 猜测资源所有权。
- Tunnel configuration 更新失败时不继续创建 DNS。
- DNS 创建失败时恢复之前的 ingress，或保留并标记为缺少 DNS。
- Access app 创建成功但 policy 失败时，保留 app ID 并允许重试。
- 删除部分失败时保留 mapping 和剩余 Cloudflare ID。

## 24. 关键实现约束

### 24.1 Tunnel 配置更新

必须使用：

```text
GET -> 合并 -> PUT -> GET 验证
```

保存前比较 Cloudflare configuration `version`，发现并发变化时停止并提示重新加载，避免静默覆盖 Dashboard 或其他客户端的修改。

### 24.2 输入校验

- 不允许用户提交任意 Cloudflare API 路径。
- 不允许用户提交任意 `cloudflared` 命令参数。
- `Bun.spawn` 使用固定参数数组，不通过 shell 拼接命令。
- hostname 必须属于所选 Zone。
- 端口限制为 `1-65535`。
- 首版 service scheme 只允许 `http` 和 `https`。
- `target_type=host` 时地址固定为 `127.0.0.1`。

### 24.3 日志

- 页面显示 Connector 运行日志和应用操作日志。
- Connector 日志保留固定数量。
- API Token 和 Tunnel token 明文存储于 SQLite，但不主动输出到页面日志和 Connector 日志。

## 25. 实施顺序

1. 建立 Bun HTTP 服务、React 页面和 SQLite 初始化。
2. 实现 Cloudflare API Client 和统一错误处理。
3. 实现 Token、Account、Zone、Tunnel 初始化流程。
4. 编写包含 Bun 和 `cloudflared` 的 Dockerfile。
5. 编写唯一的 Host Network Compose 文件。
6. 实现 Connector 子进程管理、日志和重启。
7. 实现宿主机和局域网目标构建及连通性测试。
8. 实现 Tunnel configuration 查询和安全合并。
9. 实现 DNS CNAME 创建、更新和删除。
10. 实现映射创建、列表、编辑、测试和删除。
11. 实现 Access application 和 policy 管理。
12. 实现状态聚合、失败重试和操作日志。
13. 配置后台自身的 Tunnel hostname 和 Access。
14. 分别在 Linux、macOS Docker Desktop 和 Windows Docker Desktop 验证 Host Network。

## 26. 验收标准

### Docker

- 项目只需要一份 Compose 文件即可启动。
- 容器固定使用 Host Network。
- Bun 和 `cloudflared` 在同一容器中运行。
- 容器重启后配置和映射数据不丢失。
- Connector 按设置自动恢复运行。

### Cloudflare

- 可以验证并保存 API Token。
- 可以选择 Account、Zone 和 Tunnel。
- 可以创建远程托管 Tunnel。
- 可以读取并安全更新多条 ingress。
- 可以创建和删除 Tunnel CNAME。
- 可以创建 Access application 和 Allow policy。

### 映射

- 可以把宿主机 `127.0.0.1:<PORT>` 映射到 Cloudflare hostname。
- 可以把局域网设备 `<LAN_IP>:<PORT>` 映射到 Cloudflare hostname。
- 可以在提交前测试目标连通性。
- HTTP 和 HTTPS 均可使用。
- HTTPS 可以配置 `noTLSVerify` 和 `originServerName`。

### 跨平台

- Linux Host Network 可运行。
- macOS Docker Desktop 启用 Host Networking 后可运行。
- Windows Docker Desktop 启用 Host Networking 后可运行。
- 三个平台使用相同 Compose 和相同 origin 地址规则。

## 27. 最终结论

CFTun-UI 采用单一且固定的运行模型：

```text
一份 Docker Compose
一个 Host Network 容器
一个 Bun 主进程
一个 cloudflared 子进程
一个 SQLite 数据库
一套 Cloudflare REST API 管理逻辑
```

宿主机服务统一映射为：

```text
http://127.0.0.1:<PORT>
```

局域网设备统一映射为：

```text
http://<LAN_IP>:<PORT>
```

不进行平台地址转换，不提供 Bridge 网络回退，不拆分多套 Compose。macOS 和 Windows 用户必须使用支持并启用了 Host Networking 的 Docker Desktop。
